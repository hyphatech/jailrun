jail "hypha-postgres" {
  setup {
    pg { type = "ansible"; file = "examples/postgres.yml"; }
  }
  mount {
    src { host = "."; jail = "/srv/data"; }
  }
  forward {
    pg { host = 6432; jail = 5432; }
  }
}
