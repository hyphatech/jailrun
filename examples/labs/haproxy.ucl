jail "hypha-haproxy" {
  setup {
    tcp_proxy {
      type = "ansible";
      url  = "hub://haproxy/rolling";
      vars {
        HAPROXY_TCP_MAPPINGS = "
          8080 httpserver.18-pulse-lilac.jrun 8080
        ";
      }
    }
  }
  forward {
    tcp_proxy { host = 9000; jail = 8080; }
  }
}
