# jrun ssh

SSH into the VM or a jail.

## VM

```bash
jrun ssh
```

Opens an SSH session to the FreeBSD VM.

## Jail { #jail }

```bash
jrun ssh <name>
```

Opens an SSH session directly inside a running jail.

```bash
jrun ssh postgres-16
```
